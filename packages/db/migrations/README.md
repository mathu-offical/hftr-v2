# Migrations

Generated by `pnpm db:generate` (drizzle-kit) from `src/schema/`; applied with
`pnpm db:migrate` against `DATABASE_URL`.

Log every migration here with intent (documented-from-day-one rule, m0-sprint-spec T0.3):

| # | Name | Intent |
|---|---|---|
| — | (pending first generate) | identity/billing, companies/modules/links/fund_transfers, jobs/schedules/llm accounting, numeric+temporal reference store, exchange calendars |

Notes:
- Append-only enforcement (`REVOKE UPDATE, DELETE` on `credit_ledger`, `numeric_values`,
  `calc_operations`, future `action_traces`/`verification_records`/`assistant_edits`) is applied
  in a hand-written companion migration once a dedicated app role exists (M2).
